// Stub for @scribe.js/canvas — the real package contains native .node binaries
// only needed in Node.js environments. scribe.js-ocr uses the browser Canvas API
// directly when running in a browser context.
export default {};
export const createCanvas = () => {};
export const loadImage = () => {};
export const Canvas = class {};
export const Image = class {};
