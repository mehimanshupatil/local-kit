---
import ToolLayout from '../../layouts/ToolLayout.astro';
import PDFSignTool from '../../components/pdf/PDFSignTool';
import { getToolMeta } from '../../data/tools';

const meta = getToolMeta('/pdf/sign');
---
<ToolLayout {...meta} wide>
  <PDFSignTool client:load />
</ToolLayout>
